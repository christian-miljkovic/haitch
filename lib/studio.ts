// The address the studio reads. Shown on the contact page, offered as the
// fallback when a form cannot submit, and the default recipient of staff
// alerts — so it lives in one place rather than four.
export const STUDIO_EMAIL = 'info@haitch-usa.com';
